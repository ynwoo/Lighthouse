version: '3.8'
services:
  backend:
    image: imsongj/test:0.1.0
    build:
      context: ./backend/lighthouse
      dockerfile: Dockerfile

  # frontend:
  #   image: frontend:${TAG:-latest}
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile